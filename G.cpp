#include <bits/stdc++.h>
using namespace std;
const int N = 2e5+10;

char s[N];

vector<int> num[30];
int count_num[30];

int main() {
    ios::sync_with_stdio(false);
    int t;
    cin >> t;
    while (t--){
        num->clear();
        cin >> s;
        int n = strlen(s);
        for (int i = 0; i < n; i++){
            count_num[s[i]-'a']++;
            num
        }
    }
    return 0;
}